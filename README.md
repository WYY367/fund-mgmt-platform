# 基金买入工作台

一个**本地运行的基金操作辅助工具**：记录每一次买入与卖出，并把买卖点标注在真实净值曲线上，帮助判断每次加仓相比上次是**追高**还是**抄底**、当前净值相对前次买入/卖出的涨跌幅、均线位置与区间回撤。

- 全程本地运行，数据只存在你自己电脑上，不上传云端
- 零外部依赖：无需 `npm install`，页面无框架、无 CDN、无外链字体
- 净值数据来自天天基金（东方财富）公开接口

> 本文档为**唯一文档**，同时覆盖：面向使用者的操作说明 · 项目门面介绍 · 面向开发者的架构、算法、验收与维护须知。原 `使用说明.md` 与 `交接文档.md` 已并入本文。

---

## 目录

- [一、项目简介](#一项目简介)
- [二、功能特性](#二功能特性)
- [三、快速开始](#三快速开始)
- [四、数据存储与备份](#四数据存储与备份)
- [五、数据来源与接口](#五数据来源与接口)
- [六、安全](#六安全)
- [七、项目结构](#七项目结构)
- [八、技术说明](#八技术说明)
- [九、测试与验收](#九测试与验收)
- [十、运维与维护须知](#十运维与维护须知)
- [十一、变更记录（v1.3 → v2.10）](#十一变更记录v13--v28)
- [十二、常见问题](#十二常见问题)
- [十三、免责声明](#十三免责声明)

---

## 一、项目简介

### 1.1 项目定位

一个**本地运行的基金操作辅助工具**，用于记录个人对基金的买入与卖出，并把操作点位标注在真实净值曲线上，帮助判断：

- 每次加仓相比上一次买入，是**追高**还是**抄底**
- 当前净值相对**前次买入 / 前次卖出**的涨跌幅（两个独立口径，不计算总体盈亏）
- 净值与 **顾比均线（GMMA）** 的相对位置，并给出参考操作建议
- 任意交易日在**当前所选时间段内**的回撤深度

### 1.2 技术形态

| 项 | 说明 |
|---|---|
| 形态 | 本地 Web 应用：本机启动 Node 服务，浏览器访问 `http://127.0.0.1:8765` |
| 前端 | 单文件 HTML（`public/index.html`），CSS/JS/图表/图标**全部内联**，零外部依赖 |
| 后端 | `server.js`，仅使用 Node 内置模块（`http`/`https`/`fs`/`path`/`url`），**无需 npm install** |
| 数据源 | 天天基金（东方财富）公开接口，由本地服务代理转发，规避浏览器跨域限制 |
| 存储 | **记录与净值缓存分离**：`data/records.json`（不可再生，带 rev 并发校验 + 自动快照 + localStorage 兜底）、`data/navCache.json`（可再生，单只增量写入） |
| 安全 | 仅监听 `127.0.0.1`；校验 `Host`/`Origin`，拒绝非本机页面的跨源读写；不下发任何 CORS 响应头 |
| 运行依赖 | Node.js（`C:\Program Files\nodejs` 或 PATH 中任意可用的 node） |

### 1.3 范围外（明确不做）

- **不做**登录与账号体系
- **不做**手机端的专门交互规则（页面本身是响应式的，窄屏可用，但不为移动端做特殊交互设计）
- **不做**管理后台、浏览器通知/系统推送、云端同步、视频/大文件存储

---

## 二、功能特性

### 2.1 功能清单（共 11 项）

| # | 功能 | 说明 |
|---|---|---|
| 1 | 基金选择与净值加载 | 输入 6 位基金代码或名称片段搜索；加载该基金**成立以来全部历史净值**；打开页面时若缓存已落后于当前应有净值日，**自动静默补拉**（v2.8） |
| 2 | 我的基金（表格管理） | 列出所有已加载/有记录的基金，支持切换、置顶、拖拽排序、按数值排序、更新、删除 |
| 3 | 买卖记录 | 记录每笔**买入/卖出**（日期、金额、备注），自动匹配当日净值并计算份额 |
| 4 | 批量新增 | 一次为**多只基金**添加相同的买入或卖出记录 |
| 5 | 批量管理 | 跨基金选择记录，批量改日期/改金额/改备注/删除 |
| 6 | 净值图表 | 净值曲线 + 买卖点标注 + 均线 + 区间切换 + 悬停详情 |
| 7 | 数据概览 | 操作笔数、投入本金（含卖出回款）、相较于前次买入、相较于前次卖出（共 4 张卡） |
| 8 | 数据持久化与备份 | 本地文件 + localStorage 双写；JSON 导出/导入；删除二次确认 |
| 9 | 数据源详情跳转 | 我的基金表格每行「详情」按钮 → 新标签页打开数据源站点（天天基金）的该基金详情页 |
| 10 | 前次操作涨跌幅口径 | 统一以「最新净值 vs 最后一次买入 / 最后一次卖出」计算涨跌幅（含分红），无记录留空；图表无同类前次操作时以所选区间首日为基准 |
| 11 | 待办提醒可忽略 | 「今天要处理」的每条提醒可一键忽略（不再常驻页面），支持全部忽略与恢复；忽略按「情境指纹」生效，数据变化后自动失效 |

### 2.2 净值的刷新时机（v2.8）

**手动刷新**（三种入口，效果一致，均为重拉上游全量净值并落盘）：

| 方式 | 位置 |
|---|---|
| 表格「更新」 | 「我的基金」表格该行 → 更新 |
| 待办按钮 | 顶部「净值数据超过 3 天未更新 → 刷新净值」 |
| 重新加载 | 「选择基金」输入代码 → 「加载净值」（会切换当前查看的基金） |

**自动刷新**（v2.8）：打开/刷新页面时，`boot()` 末尾自动判定缓存是否落后，落后则静默串行重拉。

> 注意：**单纯刷新浏览器页面在 v2.8 之前不会拿到新净值**——它只读本地缓存。现在会自动检查并补拉。

判定规则（宁可少拉，不可误拉）：

| 场景 | 目标净值日 | 是否自动拉取 |
|---|---|---|
| 交易日 **20:00 之后** | 当天 | 缓存最新净值日 < 当天 → **拉取** |
| 交易日 **20:00 之前** | 上一个交易日 | 缓存已含上一交易日 → 不拉 |
| 周六 / 周日 | 结论不明确 | 不拉 |
| 非交易日（法定节假日） | 按工作日推算 | 可能多拉一次（离线无法判定节假日），拉不到新数据即静默结束 |

其他约束：

- **每只基金每次打开页面只尝试一次**，失败不重试、不轮询、不弹错误
- **静默**：不显示 loading、不切换当前查看的基金；仅当确实拿到更新时提示「已自动更新 N 只基金的最新净值」
- 单次最多处理 20 只基金（`AUTO_REFRESH_MAX`），防止极端情况下打爆上游
- 上游公布时段按 `NAV_PUBLISH_HOUR = 20` 估算，早于此时间不会反复请求

**已知边界**：法定节假日无法离线判定（需交易日历），因此节假日前后的自动刷新可能多发一次请求；这只影响请求次数，不影响数据正确性。

### 2.3 收益口径（重要）

本工具**不计算总体盈亏**，只看「**当前净值相对上一次操作点**」涨了多少、跌了多少，两个口径：

- **相较于前次买入** = 最新净值 ÷ 最后一次买入的当日净值 − 1
- **相较于前次卖出** = 最新净值 ÷ 最后一次卖出的当日净值 − 1

没有买入（或卖出）记录时，对应位置**留空显示 —**。所有涨跌幅都按**含分红的复权口径**计算（用累计净值扣掉分红除权的影响）。

> 例：最后一次买入价 1.000、最后一次卖出价 1.200、最新净值 1.100 → 相较于前次买入 +10.0%，相较于前次卖出 -8.3%。

口径约定：

- 涨跌幅与盈亏**含分红**（按累计净值复权）；分红后单位净值会下跌，若只看单位净值会把分红误判成亏损或"抄底"（例：某基金单位净值 0.84 / 累计净值 11.2，纯净值口径会得出荒谬的 -92%）
- **份额按单位净值折算**（真实买入价，不含申购/赎回费）
- 均线与最大回撤仍为**单位净值口径**，分红除权日会有跳空
- 上游未提供累计净值时自动退化为单位净值口径，不崩、不误报

### 2.4 图表能告诉你什么

图表横轴是日期、纵轴是单位净值，折线是基金的真实净值曲线。每个买入点（红圆点）与卖出点（橙菱形）旁都会标**两行**数字：

```
买 +15.0%      ← 相对「上一次买入」
卖  -9.1%      ← 相对「上一次卖出」
```

- **买** 行 = 该点净值相对**上一次买入**点（追高为红、抄底为绿；颜色只作用于数字）
- **卖** 行 = 该点净值相对**上一次卖出**点
- 若某一边没有"上一次"（比如第一笔买入没有更早的买入），基准自动取**当前所选时间段的第一个交易日**，该行会标出区间名，例如 `买(近3月) +4.2%`；若基准就是该点自己，显示 `基准`（不显示 0.00%）
- **买入点**：红色圆点；**卖出点**：橙色菱形（颜色不再承载涨跌语义）

**标签自动避让**：标签位置由纯函数 `placeMarkerLabel()` 统一布置——优先放在标记点的四象限，按递增间距（10/16/24/34px）向上/向下叠排，取第一个「不越界、不压曲线、不压标记点、不压已放标签」的位置；全部被占时退化为遮挡最少的候选。只有当多笔操作挤在同几天时才可能出现轻微重叠（把鼠标悬停到点上可看清明细）。

**均线（两档可切，默认顾比）**：

| 模式 | 内容 |
|---|---|
| **顾比均线（默认）** | 短期组 EMA 3/5/8/10/12/15（青色 `#22d3ee` 组带 + 6 条细线）、长期组 EMA 30/35/40/45/50/60（品红 `#e879f9` 组带 + 6 条细线）；金叉标红▲、死叉标绿▼（可见区间内最近 8 个） |
| **关闭** | 只看净值曲线与买卖点 |

**顾比买卖建议**（仅顾比模式下，图例下方）：以最新净值为评估点，按规则链输出 买入 / 持有 / 观望 / 减仓 / 卖出 建议与理由，依据包括：价格相对长期组的位置（支撑/压力）、两组均线方向（斜率）、短期组发散/收敛、近期金叉/死叉等形态。长线趋势稳定时回调到长期组附近被视为低吸机会；短期组下穿长期组且长期组向下时提示离场。不足 60 个交易日时不给建议，面板含「不构成投资建议」免责。

**悬停详情**：鼠标在图上移动时出现**十字准线** + 跟随浮层，显示该交易日：

- 日期、单位净值、较昨日涨跌
- **最大回撤**：该日净值相对**当前所选时间段内**最高点的回撤幅度（区间高点处为 0，切换区间后重算）
- **均线读数**：顾比模式下显示短期组均值 / 长期组均值（含组内区间）与短-长乖离；关闭模式不显示均线行
- **较上次买入 / 较上次卖出**两行（该日之前最近的一次同类操作；无则基准为区间首日，并在下方注明「基准 近X月首日 <日期>」）
- 若悬停在买入/卖出点：额外显示金额、份额、持有至最新（卖出点另显示剩余份额）

**区间切换（8 档）**：近1月 / 近3月 / 近6月 / 近1年 / 近3年 / 近5年 / 近10年 / 全部（全部 = 成立至今）。切换区间会同时改变"区间首个交易日"这个兜底基准。触屏支持手指滑动查看；移出图表自动隐藏。

**图例**：基金净值 · 均线条目随模式变化（顾比：短期组 / 长期组 + 金叉 / 死叉）· **买入点** · **卖出点**（各一条，不带括号说明）；另附「较前次买入 / 较前次卖出」当前读数与口径说明（含「无上次买入/卖出记录时，基准取所选区间「近X月」的首个交易日」）。

**性能**：净值点 > 400 时顾比均线 / 组带自动抽稀采样（`gmmaStepLen = ⌈n/400⌉`，约 400 点封顶），控制 SVG 体积。

### 2.5 我的基金（表格）

列：拖拽手柄 · 基金（名称+代码，点击名称切换当前基金）· 笔数 · 净值日期 · 相较于前次买入 · 相较于前次卖出 · 成立以来最大回撤 · 顾比信号 · 操作

| 列 | 说明 |
|---|---|
| 基金 | 名称 + 代码，**点击名称切换**到该基金的图表与记录 |
| 笔数 | 该基金的记录条数 |
| 净值日期 | 该基金净值数据**更新到的最后一个交易日**（= 净值序列最后一点的日期，与待办「已 N 天未更新」同口径）；表示为 `2026-09-11`，悬停给出「净值数据截至 …」说明；未加载净值时留空「—」；不参与表头排序。右侧各列指标（相较于前次买入/卖出、最大回撤、顾比信号）均以该日净值为「最新」。**该日期早于「最近应公布的交易日」时标橙色**（提示可点「更新」重新拉取）；基准日与自动刷新同口径：交易日 20:00 后 = 当天、20:00 前 = 上一交易日、周末 = 最近的周五；只标「落后」，上游提前公布当天净值不会被误标；法定节假日无法离线判定（长假期间可能误标） |
| 相较于前次买入 | 最新净值相对**最后一次买入**净值的涨跌幅（含分红）；无买入记录时留空「—」，不参与排序（恒排末尾） |
| 相较于前次卖出 | 最新净值相对**最后一次卖出**净值的涨跌幅（含分红）；无卖出记录时留空「—」 |
| 成立以来最大回撤 | 全历史净值相对历史最高点的最深回撤（恒按全历史计算，不随图表区间变化） |
| 顾比信号 | 最近 **10 个交易日**（`GMMA_SIGNAL_WINDOW`）内出现顾比均线**金叉**（红▲，短期组均值上穿长期组均值）或**死叉**（绿▼，下穿）时标记提示；**金叉与死叉可并列显示**（以 `·` 分隔，如 `金叉 ▲ · 死叉 ▼`），同一类型出现多次时在文案后标次数（`金叉 ×2 ▲`，仅 1 次不标数字），悬停分别显示该类型次数与最近一次信号日期；无信号或历史不足 61 个交易日时留空「—」；不参与表头排序 |
| 操作 | **置顶**（星标）· **详情**（新标签页打开天天基金该基金页面）· **更新**（重新拉取净值）· **删**（删除基金及其全部记录，需二次确认） |

排序与顺序：

- **置顶**：星标按钮，置顶基金始终排在最前，行带橙色左边条
- **拖拽排序**：拖动行首 ⋮ 手柄调整顺序（桌面端；移动端隐藏手柄，用置顶替代）
- **表头排序**：点击「相较于前次买入」「相较于前次卖出」或「成立以来最大回撤」→ 从大到小；再点 → 从小到大；第三次 → 恢复拖拽手动模式（留空的基金恒排在最后）
- **更新**：重新拉取该基金净值（刷新数据、升级为全量历史）
- **详情**：新标签页打开 `https://fund.eastmoney.com/{code}.html`（净值、评级、规模、基金经理、公告等）；被浏览器拦截弹窗时退化为当前页跳转
- 顺序、置顶、排序方式均持久化保存（旧版遗留的 `profit` 排序键在载入时归一为手动模式）

### 2.6 买卖记录

录入表单：**类型（买入/卖出）· 日期 · 金额（元）· 备注**，按钮为「添加」。

记录表列：日期 · 类型（买入/卖出徽章）· 金额 · 当日净值 · 份额 · 备注 · 较上次买入 · 较上次卖出 · 持有至最新 · 操作（改/删）

- 卖出金额即卖出回款，份额 = 金额 ÷ 当日净值
- 「较上次买入」：买入点与卖出点**统一相对上一次买入价**比较（首笔买入显示「基准」）
- 「较上次卖出」：与「较上次买入」同口径，**统一相对上一次卖出价**比较；首笔卖出在其列中显示「基准」，此前尚无卖出记录的行显示「—」
- 「持有至最新」仅买入记录显示（相对最新净值的涨跌幅）
- 非交易日（周末/节假日）自动取此前最近一个交易日的收盘净值，并在净值后标注 `*`
- 表格按日期倒序显示（最新在上）

### 2.7 批量新增（多基金批量买入/卖出）

入口：「买卖记录」卡片右上角「批量新增」。流程：

1. 选择类型（批量买入 / 批量卖出）
2. 填日期、每只金额、备注
3. 勾选要操作的基金（多选）
4. 点「添加到所选基金」→ 为所有勾选基金各创建一条相同记录

与「批量管理」（批量修改/删除）相互独立，随时切换。

### 2.8 批量管理（跨基金批量编辑）

入口：「买卖记录」卡片右上角「批量管理」。能力：

- 表格列出**所有基金**的记录，支持按基金筛选
- 全选 / 清除 / 逐条勾选，实时显示已选条数
- 对选中记录批量执行：**改日期 · 改金额 · 改备注 · 批量删除**（删除需二次确认）
- 无选中项时批量操作按钮禁用
- 再点一次「退出批量」回到普通模式

### 2.9 数据概览

四张卡片：**操作笔数**（附「买 X · 卖 Y · N 次买在更低位」）、**投入本金**（附卖出回款）、**相较于前次买入**、**相较于前次卖出**（后两张为「最新净值 vs 最后一次买入 / 卖出价」，无记录时显示 — 并注明「无买入/卖出记录」，仅供参考，不再计算总体盈亏）。

### 2.10 今天要处理

页面顶部自动列出待办提示，每条附一键处理按钮：

- 净值数据超过 3 天未更新 → 「刷新净值」
- 净值数据尚未升级为成立以来全部历史 → 「升级数据」
- 有记录但该基金净值未加载 → 「加载净值」（文案按实际记录类型给出：有买入记录 / 有卖出记录 / 有买卖记录）
- 最近一次买入后跌幅 ≥ 10% → 提示可考虑补仓
- 无异常时显示「一切正常」

**忽略（消除）机制**——提醒看过即消，不常驻页面：

| 项 | 说明 |
|---|---|
| 单条忽略 | 每条提醒右侧 **×** 按钮（内联 SVG，非 emoji），点击后该条立即从页面消失 |
| 全部忽略 | 当**可忽略的提醒 ≥ 2 条**时，区块右上角出现「全部忽略」，一次写入全部消除记录 |
| 恢复 | 有待忽略项时，列表下方显示「已忽略 N 条提醒 · 恢复」；点击清除当前处于忽略状态的记录 |
| 全部忽略后的占位 | 若所有提醒都被忽略，待办区显示一条状态说明「今日待办 N 条提醒已忽略（不再常驻页面）」+「恢复」，**不留空白** |
| 持久化 | 消除记录写入 `state.dismissedTodos`，随 `data/records.json` 与 localStorage 一起保存，刷新页面、重开服务后依然生效 |
| 生效范围 | 「情境指纹」匹配才隐藏：忽略只针对**当前这一次**的情形——某只基金的净值一旦刷新过，之后再出现过期提醒会**重新提示**，不会被永久掩盖 |

**不可忽略的条目**：空态引导类提示（「开始使用：输入 6 位基金代码…」「一切正常，没有需要处理的异常」）不提供 ×——它们不是提醒，隐藏后待办区会永久空白。它们会随状态变化自动消失。

### 2.11 查看基金详情（跳转数据源）

点每行的 **详情** 按钮 → 新标签页打开**天天基金**上该基金的页面（`fund.eastmoney.com/<基金代码>.html`），可以看净值走势、阶段涨幅、评级、规模、基金经理、分红与公告。

> 该页面属于站外页面，需要本机可访问外网；如果你的浏览器拦截了新窗口，会改为在当前页跳转。

---

## 三、快速开始

### 3.1 环境要求

- Node.js（任意近期版本，仅使用内置模块）
- Windows（提供 `启动.cmd`；macOS / Linux 可直接执行 `node server.js`）
- 现代浏览器

### 3.2 启动

**方式一（Windows，推荐）**：双击目录里的 `启动.cmd`

**方式二（全平台）**：

```bash
node server.js
```

看到下面的输出即启动成功：

```
====================================================
  基金买入工作台 已启动
====================================================
  访问地址：http://127.0.0.1:8765
  记录文件：.../data/records.json
  净值缓存：.../data/navCache.json
  自动快照：.../data/backups
  按 Ctrl + C 可停止服务
====================================================
```

然后用浏览器打开 **http://127.0.0.1:8765**。建议把该地址加入书签，下次直接点书签即可（服务需保持运行）。

> 必须启动本地服务的原因：浏览器存在跨域限制，净值请求需要由 `server.js` 作为本地代理转发。

### 3.3 使用三步

1. **选基金** —— 在「选择基金」输入框填 6 位基金代码（如 `110022`），点「加载净值」。支持输入名称片段搜索。
2. **加记录** —— 在「买卖记录」里先选类型（**买入/卖出**），填日期、金额，点「添加」。
3. **看图表** —— 上方曲线立即出现你的买卖点，每个点标注**相对上一次买入的涨跌幅**。

首次打开会预置 5 条示例数据（4 买 1 卖），可直接修改或删除。

---

## 四、数据存储与备份

### 4.1 分级存储

数据按"能不能再生"分开存放，重启不会丢——记录是不可再生的资产，净值只是缓存：

| 存储位置 | 说明 |
|---|---|
| `data/records.json` | **主存储**：买卖记录（只有几 KB），原子写入，带 `rev` 版本号 + localStorage 双写 + 自动快照 |
| `data/navCache.json` | 净值缓存，属于可重建数据，单只基金增量写入；删掉后点「加载净值」即可恢复 |
| `data/backups/` | 每次写入前自动生成的记录快照（5 分钟节流，保留最近 8 份） |
| 浏览器 localStorage | 兜底备份（只存记录，不再被几 MB 净值挤爆配额），key `wb_fund_workbench_records_v2`；旧 key `wb_fund_workbench_v1` 在服务端存档成功后自动释放 |

页面顶部右上角有同步状态指示：**已保存到本地** / **保存中** / **本地模式** / **保存失败**。

- **多窗口同时用**：另一个窗口改了数据，本窗口会自动同步（`storage` 事件）；万一真撞车了，会弹窗让你选"用本页数据覆盖"还是"加载对方数据"，不会悄悄丢掉一边
- **文件损坏**：程序会把损坏文件改名保留为 `*.corrupt-*.bak` 并提示你，**不会**用示例数据把它顶掉
- **旧版本升级**：老版的 `data/store.json` 会在首次启动时自动拆分迁移为 `records.json` + `navCache.json`，原文件改名为 `store.json.migrated-*.bak` 保留（确认无误后可自行删除）
- 端口与数据目录可用环境变量覆盖：`FUND_WORKBENCH_PORT`、`FUND_WORKBENCH_DATA_DIR`（自动化测试用）

### 4.2 数据结构

```jsonc
// data/records.json
{
  "version": 2, "rev": 12,
  "records": [ { "id": "", "code": "", "type": "buy | sell", "date": "", "amount": 0, "note": "" } ],
  "currentCode": "110022", "fundOrder": [], "fundPinned": {}, "fundSort": { "key": "", "dir": "desc" },
  "dismissedTodos": {},   // 待办忽略记录 {待办标识: 情境指纹}
  "savedAt": 0
}

// data/navCache.json
{
  "version": 2,
  "funds": { "110022": {
    "name": "...", "full": true, "inceptionTs": 0, "updatedAt": 0, "fetchedAt": 0,
    "points": [ { "ts": 0, "nav": 1.0, "daily": 0.5, "ac": 1.5 } ]   // ac = 累计净值
  } }
}
```

- `dismissedTodos` 为待办忽略记录 `{待办标识: 情境指纹}`，随记录文件保存；服务端 `normalizeRecords()` 只接受字符串键值（键 ≤80 字符、最多 200 条），脏值丢弃或转字符串，字段缺省时回落为 `{}`
- 导入兼容旧版本数据（缺少 `type` 的记录视为买入；旧的 `acPoints` 序列会按时间戳合并进 `points[].ac`）

### 4.3 备份与恢复

- **备份** —— 点右上角「备份」，导出 `基金工作台备份_日期.json`。含全部基金净值和买卖记录。
- **导入** —— 点右上角「导入」，选择 JSON 文件即可恢复，条数无上限，导入与删除均需二次确认。
- 建议数据积累到 30 条以上时导出一次备份（应用会主动提醒）。
- **迁移电脑**：换机器时用「备份」导出 JSON，在新机器「导入」即可迁移。
- **自动快照回滚**：必要时把 `data/backups/` 里某份快照改名为 `records.json` 放到 `data/` 下，重启即可。

---

## 五、数据来源与接口

净值来自**天天基金（东方财富）**公开接口，覆盖**成立以来全部交易日**（不是"最近几年"），每个交易日收盘后更新。同时解析单位净值与累计净值（用于分红修正），并按时间戳对齐。

由于浏览器有跨域限制，`server.js` 作为本地代理转发请求 —— 这也是必须启动本地服务的原因。

| 接口 | 方法 | 用途 |
|---|---|---|
| `/api/ping` | GET | 健康检查（返回 `version`） |
| `/api/store` | GET | 读取记录（返回 `rev`；文件损坏时返回 `corrupt` 标记） |
| `/api/store` | POST | 保存记录（校验 `rev`，冲突返回 409 + 服务端最新数据；含待办忽略记录 `dismissedTodos`） |
| `/api/nav` | GET | 读取全部净值缓存 |
| `/api/nav?code=` | POST | 写入/更新单只基金净值（增量，不搬整包） |
| `/api/nav` | POST | 整体替换净值缓存（导入备份时使用） |
| `/api/nav?code=` | DELETE | 删除单只基金净值 |
| `/api/fund/search?key=` | GET | 基金代码或名称搜索 |
| `/api/fund/nav?code=` | GET | 基金成立以来全部净值（含累计净值 `ac`） |

> 上游请求带 15 秒超时、自动跟随重定向、非 200 直接报错。所有接口都会校验 `Host` 与 `Origin`：非本机来源一律 403。

上游若某只基金没提供累计净值，该基金的涨跌幅会自动退回单位净值口径（不影响使用，只是不体现分红）。

---

## 六、安全

本应用只在本机回环地址上运行，且：

- 只监听 `127.0.0.1`
- 校验 `Host` 头（防 DNS rebinding）与 `Origin` 头，非本机来源一律 403
- **不下发任何 CORS 响应头** —— 否则任意网站都能读写你本机的持仓数据
- 静态资源做了目录穿越防护
- `.gitignore` 排除 `data/`，个人持仓不会进入版本库

数据只存在本机 `data/` 目录与浏览器本地。联网仅用于**读取**公开净值数据，不上传任何个人信息。

---

## 七、项目结构

```
基金买入工作台/
├── 启动.cmd                    # 启动脚本（纯 ASCII + CRLF，自动探测 Node）
├── server.js                   # 本地服务：静态托管 + 接口代理 + 分级持久化 + 访问控制
├── public/
│   └── index.html              # 应用页面（单文件，CSS/JS/图表/图标全部内联）
├── data/                       # 运行时数据（.gitignore 已排除，含个人持仓，勿手改）
│   ├── records.json            # 买卖记录（主，不可再生，带 rev / savedAt）
│   ├── navCache.json           # 净值缓存（可再生）
│   ├── backups/                # 记录自动快照（保留最近 8 份）
│   └── store.json.migrated-*.bak  # 旧版单文件（迁移后保留）
├── README.md                   # 本文档（唯一文档）
├── .gitignore
├── smoke-check.js              # 静态规范自检（340 项）
└── .smoke/
    ├── server-test.js          # 服务端契约测试（40 项）
    ├── runtime-verify.js       # 运行时验证（139 项）
    ├── edge-test.js            # 边界与压力（37 项）
    └── hover-test.js           # 悬停交互专项（29 项）
```

---

## 八、技术说明

### 8.1 前端架构（必须遵守）

前端严格分层，函数调用关系构成**有向无环图**：

```
数据层  Store / NavCache  记录与服务端文件、localStorage 同步
计算层  纯函数            findNavOnOrBefore / adjReturnPct / enrichRecords / summarize
                         sliceByRange / computeDrawdownMap / computeGMMA / normalizeFunds
渲染层  render*           只读 state 更新 DOM
调度层  refreshAll()      唯一刷新入口（按固定顺序调用各渲染函数）
```

- **渲染函数之间严禁互相调用**；联动的数据变化由事件处理函数改数据后调用 `refreshAll()` 统一调度
- 需要被渲染函数调用的辅助构造函数命名不得以 `render` 开头（如 `buildBulkChecks`），以保持调用方向单向
- 启动顺序：加载数据 → 初始化状态 → `refreshAll()` 一次性渲染

### 8.2 核心算法

| 算法 | 逻辑 |
|---|---|
| 当日净值匹配 | 二分查找「日期 ≤ 目标日」的最后一个交易日净值（非交易日顺延） |
| 分红修正涨跌幅 | 令 `D = 累计净值 − 单位净值`（累计每份分红），则从 from 持有到 to 的每份价值 = `to.nav + (to.D − from.D)`；两端都有 `ac` 时才修正，否则退化为纯单位净值口径 |
| 相对上次买入涨跌幅 | 买入/卖出点均与**上一次买入**的净值点比较（含分红修正）；首次买入为基准 |
| 相对上次卖出涨跌幅 | 与上同口径，与**上一次卖出**的净值点比较（含分红修正）；首笔卖出为基准 |
| 标记相对前次操作涨跌幅 | 每个买卖点分别与「上一次买入点」「上一次卖出点」比较（含分红修正）；无同类前次操作时基准取**所选区间首个净值点**（文案标注区间名），基准即自身时记 `基准` |
| 当前 vs 前次操作（列表/概览/图例） | 最新净值 vs **最后一次买入**、**最后一次卖出**的当日净值（含分红修正）；无该类型记录时为 null（前端留空） |
| 份额法盈亏（数据层保留、界面不展示） | 剩余份额 = Σ买入份额 − Σ卖出份额；`每股价值 = 最新单位净值 + (最新累计每份分红 − 买入批次加权平均累计每份分红)`；`浮动盈亏 = 剩余份额 × 每股价值 + Σ卖出回款 − Σ买入金额`；盈亏率 = 盈亏 ÷ 买入金额 |
| 区间最大回撤 | 对所选区间做单遍扫描，记录滚动最高净值，`回撤 = (当日净值 − 滚动最高) ÷ 滚动最高`（单位净值口径） |
| 成立以来最大回撤 | 对全历史做同一计算，取最小值 |
| 移动平均 | 滑动窗口求和，窗口不足 n 个交易日时为空（图上断开、浮层显示 —）；单位净值口径 |
| 顾比均线（GMMA） | 短期组 EMA 3/5/8/10/12/15、长期组 EMA 30/35/40/45/50/60；EMA 以首个 n 点简单均值为种子、`k = 2/(n+1)`，种子点前为空（图上断开）；长序列（>400 点）自动抽稀采样控制 SVG 体积 |
| 顾比形态分析 | 规则链输出建议：近期金叉+长期组向上 → 买入；死叉+长期组向下 → 卖出；多头/空头排列 → 买入/卖出；价格入长期组带内按长期组方向给回调买入/减仓；其余给持有/观望；不足 60 个交易日不给建议 |
| 顾比信号列 | `gmmaRecentCross(fund, 10)`：复用 `computeGMMA` + `findGmmaCrosses`（短期组均值上/下穿长期组均值），窗口 = 净值序列最后 10 个交易日（`GMMA_SIGNAL_WINDOW`）；命中返回窗口内**计数汇总** `{ golden, death, count, last, lastGolden, lastDeath }`（`count = golden + death`，`last` 为窗口内最近一次交叉，`lastGolden` / `lastDeath` 为各类型最近一次的 ts）；历史不足 61 点或窗口内无交叉返回 null（列表留空） |
| 自动刷新判定（v2.8） | `expectedNavTs(now)`：非交易日（周六日）返回 `null`（不拉）；交易日 20:00 后目标 = 当天，否则目标 = 上一个工作日（向前跳过周末）。`planAutoNavRefresh()`：缓存最新净值点 `ts >= 目标` 即跳过，否则列入；已尝试过的基金（`autoRefreshTried`）跳过；上限 20 只 |
| 区间裁剪 | 按天数过滤，若不足 2 个点则回退取最后 2 个点 |

### 8.3 关键流程

**启动流程**

```
双击 启动.cmd
  └─ cd 到脚本所在目录（先切目录再切编码，保证中文路径正确）
  └─ chcp 65001（让 Node 的中文输出正常显示）
  └─ 探测 node：PATH → C:\Program Files\nodejs → 其他常见位置
  └─ 命中则执行 node server.js；未命中提示安装 Node.js
浏览器访问 http://127.0.0.1:8765
```

- `启动.cmd` 为**纯 ASCII + CRLF**（避免中文控制台 GBK 解析导致的乱码/语法错乱）
- 端口占用时服务会提示「服务已在运行，直接打开地址即可」，并以退出码 0 结束

**数据读写流程**

```
启动：GET /api/store（记录，带 rev）‖ GET /api/nav（净值缓存）
      服务端为空/损坏/离线 → 用 localStorage 兜底，服务端可用时推回
      boot 末尾 autoRefreshNav()：缓存最新净值日落后于目标日 → 静默串行重拉（每只一次）

页面保存：state（记录侧）→ serialize() → 写 localStorage（兜底）
                              └→ POST /api/store（带 rev）
                                   ├ 成功 → rev+1，快照已生成
                                   └ 409 冲突 → 用户选择：覆盖 / 加载对方数据

净值加载：GET /api/fund/nav?code= → 内存 state.funds
                              └→ POST /api/nav?code=（单只增量写入，不搬整包）

跨标签页：localStorage 写入触发另一窗口的 storage 事件 → 自动拉平
异常降级：接口失败 → 仍写 localStorage，同步状态显示「本地模式」，页面继续可用
```

**净值数据获取流程**

```
页面 → GET /api/fund/nav?code=110022
  └─ server 请求 fund.eastmoney.com/pingzhongdata/{code}.js（带 Referer，自动跟随 302）
  └─ 解析 fS_name（名称）、Data_netWorthTrend（单位净值；equityReturn 日涨跌）
  └─ 解析 Data_ACWorthTrend（累计净值 [[ts, ac]]），按时间戳对齐到每个净值点（不做位置切片）
  └─ 返回 { code, name, points[{ts, nav, daily, ac}], inceptionTs, full:true, updatedAt, divDays }
搜索：GET /api/fund/search?key=110022 → 代理 FundSearchAPI，返回 [{code, name}]
```

### 8.4 待办忽略的「标识 + 情境指纹」规则

每条可忽略的待办由两个字段决定是否隐藏：`key`（稳定标识）与 `sig`（当前情境指纹）。**只有 `dismissedTodos[key] === sig` 时才隐藏**，因此忽略是「情境级」而非「永久级」：

| 待办类型 | key | sig（指纹） | 含义 |
|---|---|---|---|
| 记录文件损坏 | `corrupt` | 隔离备份文件名 | 同一次损坏只忽略一次 |
| 净值缓存损坏 | `navCorrupt` | 隔离备份文件名 | 同上 |
| 净值超过 3 天未更新 | `stale:<code>` | 该基金**最新净值日期**的 ts | 净值一旦刷新，旧忽略失效；将来再超过 3 天会**重新提示** |
| 净值未升级为全部历史 | `notfull:<code>` | 最新净值 ts | 重新拉取过即视为新情境 |
| 有记录但净值未加载 | `nonav:<code>` | `n<该基金记录条数>` | 再添一笔记录后重新提示 |
| 最近一次买入后跌幅 ≥10% | `dip:<code>` | 该笔买入的 id | 只看跌不再重复打扰，下笔买入后重新评估 |

实现要点：

- 纯函数 `todoKey()` / `splitTodoItems()` / `collectTodoItems()` 归计算层；`renderTodayZone()` 只负责把 `splitTodoItems(collectTodoItems(), state.dismissedTodos)` 的结果画出来（渲染层不写数据）
- 数据变更（忽略 / 全部忽略 / 恢复）在交互层完成：改 `state.dismissedTodos` → `Store.save()` → `refreshAll()`；`splitTodoItems` 不回调任何渲染函数，调用链仍为 DAG
- 归一函数 `normalizeDismissedTodos()` 在前后端各有一份（服务端 `normalizeRecords()` 内联同名函数）：只保留字符串键值，键上限 80 字符、最多 200 条，防止异常数据撑大记录文件
- 忽略恢复的粒度是「当前处于忽略状态的那些 key」：`restoreTodoDismissals()` 只删这些键，不触碰其它记录

---

## 九、测试与验收

### 9.1 自动化验收（必须全绿）

| 套件 | 文件 | 验收项数 | 命令 |
|---|---|---|---|
| 静态规范自检 | `smoke-check.js` | 367 | `node smoke-check.js` |
| 服务端契约测试 | `.smoke/server-test.js` | 40 | `node .smoke/server-test.js` |
| 运行时验证 | `.smoke/runtime-verify.js` | 146 | `NODE_PATH=<node_modules> node .smoke/runtime-verify.js` |
| 边界与压力测试 | `.smoke/edge-test.js` | 37 | `NODE_PATH=<node_modules> node .smoke/edge-test.js` |
| 悬停交互专项 | `.smoke/hover-test.js` | 29 | `NODE_PATH=<node_modules> node .smoke/hover-test.js` |
| **合计** | | **619** | 全部通过（失败数 0） |

```bash
# 静态规范自检（零外链、DAG 调用链、配色、口径算法重放等）
node smoke-check.js

# 服务端契约测试（用隔离数据目录启动真实 server.js，不碰你的数据）
node .smoke/server-test.js

# 运行时 / 边界 / 悬停测试（需要 jsdom）
NODE_PATH=<已安装 jsdom 的 node_modules 目录> node .smoke/runtime-verify.js
NODE_PATH=<已安装 jsdom 的 node_modules 目录> node .smoke/edge-test.js
NODE_PATH=<已安装 jsdom 的 node_modules 目录> node .smoke/hover-test.js
```

`NODE_PATH` 指向已安装 jsdom 的目录（本次交付环境为 `C:\Users\10246\.workbuddy\binaries\node\workspace\node_modules`）。服务端契约测试会用隔离的数据目录启动真实 `server.js`（`FUND_WORKBENCH_PORT` / `FUND_WORKBENCH_DATA_DIR` 环境变量），不会碰到真实数据。

### 9.2 关键验收点

**规范类**

- 零外链：无 `<script src>`、无外部 `<link href>`、无 CDN 域名、无外部字体；图表为手写内联 SVG
  - 例外说明：基金表格「详情」按钮为**用户点击后才发生的出站跳转**（`https://fund.eastmoney.com/{code}.html`），页面本身不加载该站点任何资源，零外链约束不受影响
- 无 emoji 当图标；字体为系统字体栈
- 涨红跌绿（国内习惯）、货币统一 ¥
- 响应式：窄屏单列、表格转卡片、输入框字号 ≥16px、适配 iPhone 底部安全区
- 无登录/账号相关代码
- 页面内**禁止**出现把 `-webkit-user-drag` 置为 `none` 的全局样式（会静默压掉 `draggable="true"`，导致拖拽失效）

**功能类**

- 首次打开自动预置 5 条示例数据（4 买 1 卖），非空白页面
- 净值接口返回成立以来全量数据（实测：110022 返回 3885 个交易日，2010-08-20 起，覆盖 16.1 年）
- 区间按钮 8 档；全部 = 成立至今
- 买入点为红色圆点、卖出点为橙色菱形；每个买卖点标注**两行**（较上次买入 / 较上次卖出），标签位置经 `placeMarkerLabel` 避让（运行时断言：标签框不压曲线采样点、不互叠、不压标记点）
- 记录表为 10 列表头 / 9 列渲染（含**较上次买入 / 较上次卖出**两列）；表头排序三键 vsBuy / vsSell / **mdd** 均可用（点击 → 降序 → 升序 → 手动）
- 悬停浮层含：日期、净值、较昨日、最大回撤、均线读数、**较上次买入 / 较上次卖出两行**；买卖点额外含金额/份额/持有至最新
- 图例：买入点、卖出点各**一条**且不带括号说明；含「较前次买入 / 较前次卖出」读数与区间回退口径说明
- 基金表格为 9 列：拖拽 · 基金 · 笔数 · **净值日期** · 相较于前次买入 · 相较于前次卖出 · 成立以来最大回撤 · 顾比信号 · 操作
- 「净值日期」落后于「最近应公布的交易日」时标橙色（`--warn`）并带 `data-stale="1"`；不落后为 `data-stale="0"` 保持常规色；无净值数据时显示「—」且不标橙
- 顾比信号列为**窗口 10 个交易日**（`GMMA_SIGNAL_WINDOW = 10`）；金叉与死叉可并列显示（`·` 分隔），同类型 >1 次标 `×N`；只统计窗口内的交叉（窗口外不计入）；不参与表头排序
- 数据概览为 4 张卡（操作笔数 / 投入本金 / 相较于前次买入 / 相较于前次卖出），不再出现「浮动盈亏」
- 区间回撤语义：区间高点处 0.00%；相对高点跌 25% 处显示 -25.00%；切换区间后按新区间重算
- 均线语义：窗口不足时浮层显示 —、图上断开；第 15 点 = 前 15 日均值
- 份额法盈亏：买 1000@1.0 + 卖 500份@1.1 + 最新 1.2 → 剩余 500 份、盈亏 +150、收益率 +15%（**注**：份额法仍由 `summarize()` 计算并保留在数据层，但界面不再展示总体盈亏）
- 批量新增：勾选 N 只基金提交后记录数 +N，类型与金额正确（买入/卖出两条路径）
- 批量删除：二次确认后记录数 -1，表格同步
- 表格倒序显示（首行为最新日期）
- 拖拽排序、置顶、表头排序循环（降序→升序→手动）均生效并持久化
- 基金表格「详情」按钮：点击以新标签页打开 `https://fund.eastmoney.com/{当前行基金代码}.html`（运行时用桩函数断言调用参数）
- 记录类型徽章、旧数据自动归一（无 type 字段视为买入）
- 待办提醒忽略：每条提醒右侧有 × 按钮（带 `data-today-dismiss` / `data-sig`）；点击后该条从 DOM 消失且消除记录写入服务端 `records.json` 的 `dismissedTodos`；剩余 ≥2 条时出现「全部忽略」，点击后无可忽略项且待办区显示占位说明（不空白）；点「恢复」后提醒重现且消除记录清空（运行时 15 项断言）
- 「有记录但未加载净值」提醒的文案按实际记录类型给出（买入 / 卖出 / 买卖）；**只有卖出记录时不误称为「有买入记录」**（纯函数 `recordKindText()` 重放 4 项 + 静态 4 项）
- 忽略的「情境级」语义（纯函数重放）：标识与指纹一致 → 隐藏；指纹变化（净值已更新过）→ 旧忽略失效、重新显示；无 key 的空态文案永不参与忽略

**数据类**

- 写入 → 读回 → 落盘 一致；服务重启后数据不丢
- 接口全挂时页面可用且 localStorage 兜底保存成功（且兜底数据不含净值缓存）
- 记录文件损坏 → 隔离保留为 `*.corrupt-*.bak`，提示用户，**不覆盖、不预置示例数据**
- rev 不匹配 → 409 冲突，前端不静默覆盖，由用户选择
- 非本机 Host / Origin → 403；响应无通配 CORS 头
- 路径穿越（`/../`、`/..%2f`）无法读取 `server.js` 源码
- 旧版 `store.json` 自动拆分迁移：记录条数、基金只数、`acPoints→points[].ac` 合并均正确，原文件改名保留
- 极端数值（净值 0.0001~9999、金额 0.01~99999999）不出现 NaN/Infinity，超值自动压缩显示（亿/倍）
- 大数据量：1000 条记录 + 1300 净值点渲染 < 5 秒

**口径类**

- 分红修正：单位净值 -20% 的场景，按累计净值口径修正为实际 +10%，盈亏由 -200 修正为 +100
- 无累计净值数据（旧数据/上游缺字段）时优雅退化，与修正前结果完全一致
- 末条记录为卖出时，「最新净值相对最近一次买入」与补仓提示仍取**最后一次买入**
- 较前次买入/卖出口径：1 买 1 卖 + 最新净值场景下，两列分别为 +20% / -20%；只有卖出记录时「相较于前次买入」留空
- 标记区间回退：首笔买入（无上次买入）时基准取区间首点（-50%），第 2 笔起相对上次买入（+20%），基准即自身时标记为 `基准`
- 拖拽事件链路：`dragstart` → 行进入 dragging 态 → `drop` 到另一行 → 顺序改变且随记录持久化，同时退出数值排序模式

### 9.3 人工验收步骤

1. 双击 `启动.cmd` → 看到启动横幅与访问地址
2. 浏览器打开 `http://127.0.0.1:8765` → 首屏有示例数据、图表可见
3. 输入 `110022` 加载净值 → 图表出现完整历史曲线与均线
4. 切换区间（近1月 ↔ 全部）→ 曲线与回撤随之变化
5. 鼠标在图上移动 → 十字准线 + 浮层显示日期/净值/回撤/均线/较上次买入/较上次卖出
6. 添加一笔卖出 → 概览的卖出回款与「相较于前次卖出」随之变化；基金表格对应两列同步
7. 刷新页面（或重启服务）→ 数据仍在
8. 关闭服务窗口 → 页面显示「本地模式」，数据不丢

---

## 十、运维与维护须知

### 10.1 运行相关

- 应用必须保持本地服务运行（浏览器跨域限制决定了无法纯静态运行）；服务窗口关闭即停止服务
- 端口默认 `8765`，如需更换修改 `server.js` 顶部 `const PORT`
- 端口被占用时服务会友好提示并退出（不会报错中断）

### 10.2 数据相关

- 主数据文件 `data/records.json`（几 KB），可直接备份该文件迁移；净值缓存 `data/navCache.json` 可随时删除，下次加载净值会重建
- 自动快照在 `data/backups/`（最多 8 份，写入前生成）；必要时把某份快照改名为 `records.json` 即可回滚
- 旧版 `data/store.json` 首次启动会被拆分并改名为 `store.json.migrated-<时间戳>.bak`，确认新版数据无误后可自行删除
- 备份导入兼容旧版本数据（无 `type` 字段自动视为买入；`acPoints` 序列会按时间戳合并进 `points[].ac`）

### 10.3 数据源相关

- 依赖天天基金公开接口（`pingzhongdata`、`FundSearchAPI`）；若上游字段变更，需同步调整 `server.js` 中的解析正则
- 累计净值取 `Data_ACWorthTrend`（`[[时间戳, 累计净值]]`），按时间戳与单位净值对齐；上游若去掉该变量，前端会自动退化为纯单位净值口径（不崩、不误报分红）
- 上游请求带 15 秒超时、自动跟随重定向、非 200 直接报错

### 10.4 维护约束

- 新增功能时保持：单文件全内联、零外部依赖、图表手写 SVG、`refreshAll()` 统一调度、渲染函数互不调用
- 函数调用链必须保持无环（`smoke-check.js` 会校验）；需要互相触发的逻辑走"改数据 → refreshAll"
- 每次改动后必须跑通**五套测试（619 项）**再交付
- 新增/修改 API 时同步更新 `.smoke/server-test.js` 的契约断言

### 10.5 已知边界

- 拖拽排序仅桌面端可用（移动端以置顶替代）——实现依赖浏览器原生 HTML5 拖放；页面内**禁止**出现把 `-webkit-user-drag` 置为 `none` 的全局样式，`smoke-check.js` 已加断言防回归
- 记录类型仅买入/卖出两种
- 图表最大回撤在「全部」区间下等于成立以来回撤；表格列的「成立以来最大回撤」恒按全历史计算，不随区间变化
- 分红修正为**现金分红口径**（分红的现金计入收益），未模拟红利再投的份额增长，也未考虑申赎费与税费
- 卖出未校验"份额是否超过持仓"，输错金额会产生失真盈亏（待办）
- 界面已不再展示总体盈亏（份额法 `summarize()` 仍保留在数据层，作为「买在更低位」等统计与后续扩展的基础）
- 定投场景下「相较于前次买入/卖出」是**点对点**比较，不反映加权成本；如需资金加权收益（XIRR）属待办
- 均线与最大回撤仍为单位净值口径，分红除权日存在跳空
- 顾比买卖建议由固定规则从均线形态推得，**仅供参考，不构成投资建议**；均线是滞后指标，对突发事件与消息面无效；建议随最新净值实时重算，不同交易日结论可能反转
- 顾比均线 / 组带在净值点 > 400 时做抽稀采样（约 400 点封顶），均线为平滑曲线、视觉无差；若上游改为输出锯齿状数据需重新评估采样步长
- 「详情」按钮跳转的是站外页面，其内容与可用性由天天基金决定；本机无法访问外网时点击不会有内容
- 记录表「较上次买入」列仍是逐笔两两比较、首笔显示「基准」，未采用图表的区间回退规则（记录表无"区间"概念）
- `index.html` 中残留少量 `data-page-node-id` 属性（历史编辑器产物），未被任何逻辑引用，可安全清理
- 待办忽略是**情境级**：忽略「净值已 N 天未更新」后，该基金净值一旦刷新、之后再超过 3 天仍会重新提示；但若上游长期不更新（净值日期不变），忽略会一直生效——需要找回时点「恢复」
- 空态引导类提示（开始使用 / 一切正常）不提供忽略按钮，属有意设计（否则待办区会永久空白）
- `dismissedTodos` 中不再匹配任何当前待办的旧键会保留在文件里（最多几百字节，惰性数据），不影响渲染；未做主动 GC
- **自动刷新无法离线判定法定节假日**（需交易日历）：节假日当天按工作日推算，可能多发一次请求，拉不到新数据即静默结束——只影响请求次数，不影响数据正确性
- **自动刷新只在打开/刷新页面时触发**：页面一直挂着不会自动更新（不轮询）；要拿最新净值需刷新页面或手动点「更新」
- **自动刷新失败不重试**：某只基金若因网络抖动/上游异常没拉到，本次不再重试，可手动点「更新」补
- **`gmmaRecentCross()` 的 `lastGolden` / `lastDeath` 为补充字段**：原 v2.7 源码曾随工作区回退丢失，按变更记录重建时为支撑「按类型显示最近一次信号日期」而新增；原 `golden` / `death` / `count` / `last` 四个字段语义不变
- **「净值日期」标橙同样无法离线判定法定节假日**：长假期间基准日会按工作日推算，可能出现「其实已是最新、却被标橙」的假警报；只影响颜色提示，不影响数据与涨跌幅口径。忽略它的方式是知道当天是节假日（春节 / 国庆等固定长假尤其明显）
- **QDII / 港股通类基金会被持续标橙**：这类基金净值天然滞后 1–2 个交易日，判定只看「净值日期 vs 应公布交易日」，不做基金类型识别（上游未提供可靠字段），因此属于已知误报
- **「净值日期」的落后判定只在页面渲染时计算一次**：页面一直挂着不刷新时，基准日不会随时间跨过 20:00 / 周末而自动重算（无定时器）；刷新页面或任意一次数据变更触发的 `refreshAll()` 即会重新判定
- **标橙只表示「净值日期落后」**：不判断该基金是否已停止更新、也不代表持仓盈亏有任何变化，与待办「净值数据超过 3 天未更新」是两套独立提醒（阈值不同：这里是 1 个交易日）

---

## 十一、变更记录（v1.3 → v2.10）

### 11.1 v1.3 → v2（按"第一性原理"审查后处理的 P0 问题）

| # | 问题 | 处理 |
|---|---|---|
| 1 | 个人持仓数据被提交并推送到 GitHub | 新增 `.gitignore`、`git rm --cached`、重写全部历史并强推；原文件已从历史中清除（GitHub 对不可达对象的回收另有延迟，必要时删库重建） |
| 2 | 损坏文件会被示例数据覆盖，销毁唯一副本 | 服务端隔离改名保留 + 返回 `corrupt` 标记；前端不再预置示例数据并给出提示 |
| 3 | 多标签页 last-write-wins 静默丢记录 | rev 并发校验 + 409 冲突由用户决策 + `storage` 事件跨页同步 |
| 4 | 通配 CORS，任意网页可读写本机持仓 | 移除 CORS 响应头 + `Host`/`Origin` 校验 |
| 5 | 未使用累计净值，分红被当成下跌 | 解析并按时间戳对齐 `ac`，涨跌与盈亏改为含分红口径；无数据时优雅退化 |
| 7 | 末条为卖出时「最近一次买入」静默失效 | 改为定位最后一次**买入**记录（图例、今日待办、卡片一致） |
| 9 | 净值缓存与记录混装，双写濒临配额 | 拆分 `records.json` / `navCache.json`，净值单只增量写入；记录侧仅 1.5KB |

顺带修复：`escapeHtml` 的 `>` 转义规则曾被一段外部注入文本破坏（`index.html:929`，实测不转义 `>`），已还原并加入断言防回归。

尚未处理（原审查中的 P1/P2 项）：卖出份额校验（#6）、XIRR（#8）、真值对照测试（#10）、过期提示按交易日历（P2）、上游请求缓存、`window.prompt` 编辑体验、清理 `data-page-node-id`。

### 11.2 v2 → v2.1（新增「数据源详情跳转」）

| 变更 | 文件 | 说明 |
|---|---|---|
| 基金表格新增「详情」按钮 | `public/index.html` | 操作列第 2 个按钮（置顶 · **详情** · 更新 · 删），操作列宽 150px → 230px；点击在新标签页打开 `https://fund.eastmoney.com/{code}.html`，弹窗被拦截时退化为当前页跳转 |
| 新增函数 | `public/index.html` | `fundDetailUrl(code)`（纯函数，拼地址）与 `openFundDetail(code)`（打开新标签页），由 `fundBody` 事件委托的 `data-act="source"` 分支调用；不参与渲染层调用链 |
| 新增断言 | `smoke-check.js` / `.smoke/runtime-verify.js` | 静态断言 8 项 + 运行时断言 2 项 |
| 测试总数 | 五套 | 314 → **324 项**，全部通过 |

出站链接不破坏「零外链」：页面自身仍不加载任何外部脚本/样式/图片，跳转只在用户点击后发生。

### 11.3 v2.1 → v2.3

**11.3.1 拖拽排序修复（v1.6）**

**症状**：基金列表拖 ⋮ 手柄无反应（Chrome / Edge 均复现）。

**根因**：`public/index.html` 头部残留一条设计器样式 `<style id="__dm_no_drag_style__">* { -webkit-user-drag: none !important; }</style>`。`*` + `!important` 命中所有元素，而该私有属性的 `none` 取值语义为「禁止该元素被拖拽」（WebKit 官方文档），会压掉行的 `draggable="true"`，原生拖放因此静默失效。

| 变更 | 文件 | 说明 |
|---|---|---|
| 删除残留样式块 | `public/index.html` | 移除 `__dm_no_drag_style__`（及同源的 `__dm_bindable_badge_style__`） |
| 显式声明可拖拽 | `public/index.html` | 新增 `.funds tr[draggable="true"], .drag-handle { -webkit-user-drag: element; }` |
| 新增断言 | `smoke-check.js` / `.smoke/runtime-verify.js` | 静态 5 项 + 运行时 7 项（加载第二只基金 → `dragstart` → `drop` → 顺序改变并落盘 → 切回） |

**11.3.2 「浮动盈亏」改为「相较于前次买入 / 相较于前次卖出」（v2.2）**

取消总体盈亏口径，全部改为**当前净值相对上一次操作点**的涨跌幅（含分红修正）：

| 位置 | 变更 |
|---|---|
| 我的基金表格 | 「浮动盈亏」单列 → **两列**（相较于前次买入 / 相较于前次卖出），均可点表头排序；无该类型记录时留空「—」 |
| 数据概览 | 三张卡 → **四张**（操作笔数 / 投入本金 / 相较于前次买入 / 相较于前次卖出），卡片副文案给出基准日期 |
| 图表买卖点 | 标签改为**两行**：`买 …` / `卖 …`；买入点固定红色圆点、卖出点橙色菱形（颜色不再承载涨跌语义） |
| 图表悬停 | 任意交易日浮层都显示「较上次买入 / 较上次卖出」两行 |
| 图例 | 买入点、卖出点各**一条**且去掉括号说明；保留「较前次买入 / 较前次卖出」读数与口径说明 |
| 无前次操作时 | 图表类场景退化为**所选区间首个净值点**为基准，文案标注区间名（`买(近3月) +5.2%`）；列表/概览类场景**留空** |
| 新增纯函数 | `latestVsLastAction()`（列表/概览）、`compareMarkersToPrevActions()`（标记）、`computeVsActionsByDate()`（悬停）、`rangeLabel()` |
| 旧数据兼容 | 载入时把遗留的 `fundSort.key = 'profit'` 归一为手动模式 |

**测试总数**：324 → **373 项**，全部通过（177 / 36 / 101 / 37 / 22）。

**11.3.3 记录表补列 · 标签避让 · 排序键修复（v2.3）**

| # | 问题/需求 | 处理 |
|---|---|---|
| 1 | 买卖记录表缺「较上次卖出」 | `enrichRecords()` 增加 `vsPrevSell`（与 `vsPrev` 同口径，相对上一次卖出价，含分红修正）；记录表新增该列，首笔卖出显示「基准」，此前无卖出的行显示「—」；表格变为 10 列 |
| 2 | 买卖点标签可能遮挡曲线/标记 | 新增纯函数 `pointInRect()` / `rectsOverlap()` / `segIntersectsRect()`（Liang-Barsky）与 `placeMarkerLabel()`：四象限 × 递增间距（10/16/24/34px）× 向上/向下叠排，取第一个「不越界、不压曲线、不压标记点、不压已放标签」的位置，全部被占时取遮挡最少者；渲染改为两遍（先画点备文案，再统一布置标签，避让集合含全部标记点） |
| 3 | 点击「最大回撤」表头无法排序 | 根因：排序键 `data-sort="dd"` 与指标字段名 `mdd` 不一致。统一改为 `data-sort="mdd"`；载入归一只认 `vsBuy / vsSell / mdd`（旧 `profit` / `dd` 归一为手动模式） |

**测试总数**：373 → **414 项**，全部通过（200 / 36 / 119 / 37 / 22）。

### 11.4 v2.3 → v2.4（新增顾比均线与买卖建议）

| 变更 | 文件 | 说明 |
|---|---|---|
| 均线三档切换 | `public/index.html` | 工具栏右侧新增 `maModeGroup`（`state.maMode` = `gmma`（默认）/ `classic` / `none`，会话级不持久化）；顾比模式绘制短期组 EMA 3/5/8/10/12/15（青色 `#22d3ee`）与长期组 EMA 30/35/40/45/50/60（品红 `#e879f9`）的组带 + 12 条细线，金叉红▲ / 死叉绿▼（可见区间最近 8 个，`<clipPath id="plotClip">` 裁剪越界段） |
| 新增计算层 | `public/index.html` | `computeEMA()`（首个 n 点简单均值为种子，`k = 2/(n+1)`，种子点前为空）、`computeGMMA()`、`gmmaGroupStats()` / `computeGmmaBandStats()`、`findGmmaCrosses()`、`analyzeGmmaAt()`（规则链）、`gmmaAdviceHtml()`；均为纯函数，不进渲染调用链 |
| 顾比建议面板 | `public/index.html` | 图例下方 `#gmmaAdvice`（仅 gmma 模式显示），输出建议、理由列表、关键读数，含「不构成投资建议」免责 |
| SVG 抽稀采样 | `public/index.html` | 净值点 > 400 时均线 / 组带按 `gmmaStepLen = ⌈n/400⌉` 抽稀（段首段尾保留、null 断开保留）；1313 点下 SVG 210KB → 89KB，jsdom 单次渲染 ~90ms → ~50ms |
| 测试同步 | 五套 | 静态 13 项 + 纯函数重放；图表段改模式感知 |

**修正的遗留问题**（与顾比功能无关，属旧断言与实际不符）：记录表列数断言 10 → 9；区间按钮选择器限定 `#rangeGroup .range-btn`（避免混入均线模式按钮）；`edge-test.js` 渲染耗时断言排除固定等待（`bootMs = elapsed − 2500`）。

**测试总数**：414 → **465 项**，全部通过（235 / 36 / 125 / 37 / 32）。

### 11.5 v2.4 → v2.5（新增基金列表「顾比信号」列）

| 变更 | 文件 | 说明 |
|---|---|---|
| 基金表格新增「顾比信号」列 | `public/index.html` | 列位于「成立以来最大回撤」与「操作」之间（表格 7 → 8 列）；最近 5 个交易日内出现金叉显示红「金叉 ▲」、死叉显示绿「死叉 ▼」，悬停显示信号日期与口径；无信号 / 历史不足 61 个交易日时留空「—」；不参与表头排序 |
| 新增计算层纯函数 | `public/index.html` | `GMMA_SIGNAL_WINDOW = 5` 与 `gmmaRecentCross(fund, days)`：复用 `computeGMMA` + `findGmmaCrosses`，窗口 = 净值序列最后 N 个交易日（idx ∈ [n−N, n−1]），返回窗口内最近一个交叉或 null |
| `fundMetrics()` 扩展 | `public/index.html` | 新增 `gmmaSignal` 字段（= `gmmaRecentCross(fund, GMMA_SIGNAL_WINDOW)`），由 `renderFundTable()` 读取渲染 |
| 清理 v2.4 遗留失效断言 | `.smoke/runtime-verify.js` / `.smoke/hover-test.js` | 删除依赖已下线经典模式的断言；修复 `hover-test.js` classic 按钮 null 崩溃（`dispatchEvent` on null 导致整套测试中途异常退出） |
| 新增断言 | `smoke-check.js` / `.smoke/runtime-verify.js` | 静态 11 项 + 运行时 2 项 |

**测试总数**：465 → **467 项**，全部通过（241 / 36 / 124 / 37 / 29）。

### 11.6 v2.5 → v2.6（新增「今天要处理」提醒可忽略）

| 变更 | 文件 | 说明 |
|---|---|---|
| 待办项改为 `key + sig` 结构 | `public/index.html` | 每条提醒带稳定标识与情境指纹；`state.dismissedTodos[key] === sig` 时才隐藏，数据变化后旧忽略自动失效。清理逻辑从 `renderTodayZone()` 拆出为纯函数 `collectTodoItems()` / `todoKey()` / `splitTodoItems()` |
| 单条忽略（×） | `public/index.html` | 每条提醒右侧新增 × 按钮（内联 SVG，非 emoji），带 `data-today-dismiss` / `data-sig`；点击写入消除记录 |
| 全部忽略 | `public/index.html` | 可忽略项 ≥2 条时，区块右上角渲染 `.todo-head` 内的「全部忽略」按钮（`data-today-dismiss="all"`） |
| 恢复入口 | `public/index.html` | 忽略后列表下方显示「已忽略 N 条提醒 · 恢复」；全部被忽略时待办区改为状态占位 + 恢复按钮，不留空白 |
| 数据变更函数 | `public/index.html` | 交互层新增 `dismissTodoItems()` / `dismissOneTodo()` / `dismissAllTodos()` / `restoreTodoDismissals()`：改数据 → `Store.save()` → `refreshAll()`，保持渲染层只读、调用链无环 |
| 持久化 | `public/index.html` | `state.dismissedTodos` 纳入 `Store.serialize()` / `Store.applyData()`；新增归一函数 `normalizeDismissedTodos()`；导入旧备份（无该字段）时回落为空对象 |
| 服务端契约 | `server.js` | `emptyRecords()` 增加 `dismissedTodos: {}`；`normalizeRecords()` 通过 `normalizeDismissedTodos()` 白名单化该字段 |
| 样式 | `public/index.html` | 新增 `.todo-head` / `.todo-foot` / `.tip-x` 三条规则（× 高 22px、忽略/恢复按钮高 24px） |
| 新增断言 | `smoke-check.js` / `.smoke/runtime-verify.js` / `.smoke/server-test.js` | 静态 15 项 + 纯函数重放 11 项；运行时 15 项；契约 4 项 |

设计取舍（已与使用方确认）：

1. **忽略范围**：所有「提醒」类待办都可忽略，但空态引导类（开始使用 / 还没有记录 / 一切正常）不提供 ×
2. **重现规则**：按「情境指纹」失效，即该基金净值更新过、之后再出现过期会重新提示；期间完全静默
3. **粒度**：单条 × + 顶部「全部忽略」，并提供可逆的「恢复」入口

**测试总数**：467 → **512 项**，全部通过（267 / 40 / 139 / 37 / 29）。

> 未做视觉截图验证（本地浏览器未接入自动化），行为与结构由 jsdom 运行时断言覆盖；首次双击 `启动.cmd` 即可直接看到效果。

### 11.7 v2.6 → v2.7（顾比信号列改为「窗口 10 日 + 交叉计数」）

| 变更 | 文件 | 说明 |
|---|---|---|
| 信号窗口 5 → **10 个交易日** | `public/index.html` | `GMMA_SIGNAL_WINDOW = 5` → `10`，覆盖范围扩大一倍 |
| 返回值由单个交叉改为**计数汇总** | `public/index.html` | `gmmaRecentCross()` 由「返回窗口内最近一个交叉 `{idx, ts, type}`」改为 `{ golden, death, count, last, lastGolden, lastDeath }`；`count = golden + death`，`last` 仍为窗口内最近一次交叉，`lastGolden` / `lastDeath` 为各类型最近一次的 ts（用于按类型给出最近日期） |
| 列表渲染支持**多信号并列** | `public/index.html` | 金叉、死叉各自独立成块，同时存在时以 `·` 分隔（`金叉 ▲ · 死叉 ▼`）；同一类型 >1 次时文案后标 `×N`（`金叉 ×2 ▲`），仅 1 次时不标数字；悬停分别给出该类型次数与最近一次信号日期 |
| 空值容错 | `public/index.html` | `toDateStr(null \| '')` 返回空串，避免生成 `Invalid Date` 文案 |
| 测试同步 | `smoke-check.js` | v2.7 新增 12 项（静态 8 + 重放 4）；v2.5 段落升级 7 项（原断言在窗口 5 日下其实是空转通过，已改为真正命中窗口内的确定性数据，并补「交叉存在但落在窗口外 → 返回 null」的反例）；窗口常量断言 5 → 10 |
| 文档同步 | `README.md` | 功能清单、算法表、测试项数同步更新 |

设计取舍：

1. **窗口取 10 日**：与 `analyzeGmmaAt()` 里「近期新鲜信号」的 10 日口径一致，避免同一份交叉在建议面板与列表列上采用两套时间尺度
2. **计数只按窗口内统计**：10 日窗口之外的历史交叉不计入，列表列表达的是「最近这段有没有反复穿越」
3. **单次不标数字**：只在同类型出现 ≥2 次时补 `×N`，保持单信号场景的文案与旧版一致，不引入视觉噪音
4. **不参与表头排序**：维持原设计（非数值指标）

> 实现说明：原 v2.7 源码曾随工作区回退丢失，本节按变更记录描述重新实现。为使「悬停给出该类型最近一次信号日期」成立，`gmmaRecentCross()` 的返回在上述 4 个字段（`golden` / `death` / `count` / `last`）之外**补充**了 `lastGolden` / `lastDeath`——纯增量，不改变原字段语义。

**测试总数**：512 → **531 项**，全部通过（286 / 40 / 139 / 37 / 29）。

### 11.8 v2.7 → v2.8（打开页面自动刷新净值）

| 变更 | 文件 | 说明 |
|---|---|---|
| 新增自动刷新判定 | `public/index.html` | 纯函数 `isTradingDayTs()` / `expectedNavTs()` / `planAutoNavRefresh()` 与调度 `autoRefreshNav()`（常量 `NAV_PUBLISH_HOUR = 20`、`AUTO_REFRESH_MAX = 20`、会话级 `autoRefreshTried`） |
| 接入启动流程 | `public/index.html` | `boot()` 两条路径（需静默加载 / 已有缓存）末尾都调用 `autoRefreshNav()`；串行 `loadFundNav(code, true, true)`，仅确拿到更新时 `toast` 一条 |
| 新增断言 | `smoke-check.js` | 静态 7 项 + 真实重放 20 项（交易日判定 4 · 目标净值日 8 · 刷新计划 6 · 上限 1 · 会话去重 1） |
| 文档 | `README.md` | 新增 2.2 节「净值的刷新时机」，2.x 小节顺次后移；测试项数与维护约束同步 |

设计取舍：

1. **判定宁紧勿松**：交易日 20:00 前宁可认为「今天的净值还没出来」，也不判为过期——避免每天晚上反复请求上游
2. **不止于"有没有新净值"**：不做「先探测再决定」的两段式请求（探测本身就等于拉全量，成本翻倍），直接把缓存里最后一点的时间戳与目标日比较
3. **静默优先**：不弹 loading、不弹错误、不切换当前基金；只有真的更新了才说一句
4. **失败不重试**：每只基金每次打开页面只试一次，把「上游挂了」的代价限制在一次请求内

**测试总数**：531 → **558 项**，全部通过（313 / 40 / 139 / 37 / 29）。

### 11.9 修正：「有记录但未加载净值」提醒的文案与事实不符

**症状**：某只基金**只有卖出记录**（没有任何买入）而净值尚未加载时，待办区仍提示「基金 X **有买入记录**，但净值数据尚未加载」。

**根因**：`collectTodoItems()` 第 2 段统计的是 `state.records` 里的**全部**记录（含卖出），但文案把类型写死为「买入记录」——判定与文案不一致。

| 变更 | 文件 | 说明 |
|---|---|---|
| 记录按基金分别计数 | `public/index.html` | 先统计各基金的买入 / 卖出条数（`recStat[code] = { buy, sell }`），再逐只判定是否缺净值 |
| 新增纯函数 `recordKindText()` | `public/index.html` | 由条数给出文案类型名：买入 / 卖出 / 买卖三者兼有时 → 「买卖记录」 |
| 文案改为按类型拼接 | `public/index.html` | 「基金 X 有**买入记录 / 卖出记录 / 买卖记录**，但净值数据尚未加载」 |
| 情境指纹不变 | `public/index.html` | 仍为 `'n' + 记录条数`，忽略语义与持久化结构均未变 |
| 新增断言 | `smoke-check.js` | 静态 4 项 + 纯函数重放 4 项 |

判定逻辑本身**不改**：卖出记录同样需要净值才能折算份额，保留在提醒范围内是正确行为。

**测试总数**：558 → **566 项**，全部通过（321 / 40 / 139 / 37 / 29）。

### 11.10 删除待办区的「《X》还没有买卖记录」引导

**变更**：待办区不再单独提示「《X》还没有买卖记录，在下方添加第一笔」。

| 项 | 说明 |
|---|---|
| 删除的分支 | `collectTodoItems()` 末尾空态中，原「当前基金无记录」分支（含 `hasRec` 判定）整体移除 |
| 行为变化 | 无待办且无当前基金 → 仍显示「开始使用：输入 6 位基金代码…」；无待办且已选基金（**无论有无记录**）→ 统一显示「一切正常，没有需要处理的异常」 |
| 未受影响 | 买卖记录表自身的空态（`#recEmpty`，「还没有买卖记录，在上方添加第一笔」）保留；空态仍不带 ×（不可忽略） |
| 新增断言 | `smoke-check.js` 4 项（待办区不再产出该文案、不再有 `hasRec` 分支、记录表空态保留、已选基金时落「一切正常」） |

**测试总数**：566 → **570 项**，全部通过（325 / 40 / 139 / 37 / 29）。

### 11.11 v2.8 → v2.9（基金列表新增「净值日期」列）

**需求**：在「我的基金」列表中直接看到每只基金的净值数据更新到了哪一天。

| 变更 | 文件 | 说明 |
|---|---|---|
| 基金表格新增「净值日期」列 | `public/index.html` | 列位于「笔数」与「相较于前次买入」之间（表格 8 → 9 列，宽 108px）；内容为该基金净值序列**最后一点**的日期（`YYYY-MM-DD`、等宽数字），悬停给出「净值数据截至 …（该基金最新净值日，右侧指标均以此为最新）」；未加载净值时留空「—」；不参与表头排序 |
| `fundMetrics()` 扩展 | `public/index.html` | 新增 `navDate`（展示用日期串）与 `navTs`（原始时间戳，便于后续排序/比较）：`var latest = latestPoint(fund.points)` → `toDateStr(latest.ts)`；无净值点或未加载净值时分别为 `''` / `null`，渲染为「—」 |
| 静态占位行对齐 | `public/index.html` | 初始 `<tbody>` 占位行补齐为 9 个 `<td>`（原先缺「顾比信号」单元格），与表头列数一致 |
| 新增断言 | `smoke-check.js` / `.smoke/runtime-verify.js` | 静态 9 项（列位置、口径 title、悬停文案、等宽数字、列头与占位行列数一致、不参与排序等）+ `fundMetrics` 真实重放 6 项（取最后一点、多基金不串号、净值点为空、未加载净值、可独立执行）；运行时 4 项（表头含该列且位置正确、单元格为 `YYYY-MM-DD`、与净值缓存最后一点日期一致、带「净值数据截至」悬停说明）；原「8 列」断言改为 9 列 |
| 文档同步 | `README.md` | 2.5 节列说明与验收项、9.1/9.2 测试项数同步 |

设计取舍：

1. **口径取「最新净值日」而非「本地拉取时间」**：用户关心的是「数据更新到哪天」，这也是待办「净值数据超过 3 天未更新」的判定依据；拉取时间属实现细节，不进入表格
2. **放在「笔数」之后、数值列之前**：净值日期是右侧各列指标（较前次买入 / 较前次卖出、最大回撤、顾比信号）的「as of」基准，置于其前符合阅读顺序
3. **不参与表头排序**：与「顾比信号」列一致，既有排序键（`vsBuy` / `vsSell` / `mdd`）语义不变
4. **不做过期高亮**：过期提醒已由「今天要处理」承担且支持忽略，表格内再加颜色会造成双重提示

**测试总数**：570 → **589 项**，全部通过（340 / 40 / 143 / 37 / 29）。

### 11.12 v2.9 → v2.10（净值日期落后于「应公布的交易日」时标橙）

**需求**：基金净值日期不等于「最近应公布的交易日」时，把「净值日期」标成橙色。

| 变更 | 文件 | 说明 |
|---|---|---|
| 新增基准日纯函数 | `public/index.html` | `dueNavTs(now)`：复用 `expectedNavTs()`（交易日 20:00 后 = 当天 / 20:00 前 = 上一交易日），仅多一步周末兜底——`expectedNavTs()` 周末返回 `null`（那是「不该发请求」的语义），而展示需要一个确定的基准日，故回退到最近的周五 |
| 落后判定 | `public/index.html` | `fundMetrics(code, now)` 新增 `navDue` / `navStale`；判定为 `navTs < dueNavTs(now)`，**严格「早于」而非「不等于」**——交易日 20:00 前上游个别基金可能已提前公布当天净值，此时 `navTs > due` 属数据更新，不该标橙 |
| 渲染标橙 | `public/index.html` | 落后者用 `color:var(--warn)`（橙色，与置顶星标同色）+ `font-weight:600`，并带 `data-stale="1"`；未落后者 `data-stale="0"` 保持常规色；无净值数据仍走「—」分支，不标橙 |
| 悬停说明 | `public/index.html` | 标橙时提示「净值数据截至 X，落后于最近应公布的交易日 Y；可点『更新』重新拉取（法定节假日可能误标）」；列头 title 同步说明标橙含义 |
| 注释纠偏 | `public/index.html` | `expectedNavTs()` 的原注释写「周末：目标回退到最近一个周五」，与实现（返回 `null`）不符，已改正并在 `dueNavTs()` 处说明两者分工 |
| 新增断言 | `smoke-check.js` / `.smoke/runtime-verify.js` | 静态 11 项（函数存在、口径复用、周末兜底、严格早于、`--warn` 配色、`data-stale`、悬停与列头文案、`—` 分支不标橙）+ 基准日重放 7 项（周一/周二 × 20:00 前后、周六、周日、周五 21:00）+ `fundMetrics` 重放 8 项（落后 / 相等 / 20:00 前基准回落 / 提前公布 / 周末判定 / 空净值 / 未加载净值）；运行时 3 项（`data-stale` 存在、`data-stale=1 ↔ 橙色`一致、标橙时悬停说明含基准日） |
| 文档同步 | `README.md` | 2.5 节列说明、9.2 验收点、10.5 已知边界、FAQ 各补一条 |

设计取舍：

1. **基准日与自动刷新同口径**：不再另造一套「最近交易日」定义。若按字面理解（含今天），则每个交易日 20:00 前所有基金都会变橙——那是假警报，不是提示
2. **只标「落后」，不标「不等」**：`<` 与 `!==` 的差别只在「上游提前公布当天净值」这一种情形，后者会误标，前者不会
3. **沿用 `--warn`**：与置顶星标同为警示色，不新增色板、不改动涨跌红绿色
4. **不做过期天数阈值**：落后 1 个交易日即标橙（阈值更紧的「超过 3 天未更新」仍由待办承担），两者互不替代
5. **不加定时器**：基准日随渲染重算，不给页面增加轮询；跨过 20:00 后刷新页面即生效

**测试总数**：589 → **619 项**，全部通过（367 / 40 / 146 / 37 / 29）。

---

## 十二、常见问题

**Q：净值什么时候刷新？刷新浏览器页面能拿到当天新净值吗？**

能（v2.8 起）。打开/刷新页面时会自动检查：若缓存里最新净值日已落后于「当前应有了的净值日」，就静默重拉一次；拉到了会提示「已自动更新 N 只基金的最新净值」，没落后则完全安静。

判定时机：**交易日 20:00 之后**打开页面才会去拿当天净值（此前上游还没公布）；周末打开不会请求上游。

想立刻手动刷新，有三个入口（见 2.2 节）：表格「更新」、顶部待办「刷新净值」、重新输入代码「加载净值」。

**Q：为什么晚上 8 点前打开页面，净值还是昨天的？**

因为上游（基金公司）通常要 20:00–23:00 才陆续公布当天净值。此时「昨天的净值」就是当前最新值，程序不会去请求，也不会误报为过期。QDII / 港股通类基金滞后 1–2 个交易日。

**Q：为什么某只基金的「净值日期」是橙色的？**

说明该基金的净值日期**早于「最近应公布的交易日」**，即它的数据比应该有的落后了，可以点该行的「更新」重新拉取。基准日与自动刷新同一口径：交易日 20:00 后 = 当天、20:00 前 = 上一交易日、周末 = 最近的周五——所以早上打开页面时，显示上一交易日并不算落后，不会标橙。

需要留意两点：一是 QDII / 港股通类基金本身滞后 1–2 个交易日，会在多个交易日内持续标橙，属正常；二是**法定节假日无法离线判定**，长假（春节 / 国庆）期间可能把「其实已是最新」的日期误标为橙色，节后第一个交易日恢复正常。

**Q：橙色和待办里的「净值数据超过 3 天未更新」是一回事吗？**

不是。表格标橙的阈值更紧（落后 1 个交易日即提示），只影响该行颜色、不产生待办；待办里那条阈值是 3 天，可一键「刷新净值」也可忽略。两者独立，互不替代。

**Q：打开页面时看到它自动请求了上游，会不会被限流？**

每只基金每次打开页面只请求一次、失败不重试，单次上限 20 只，且只在确实落后时才请求。正常使用一天一个页面会话只会有 1 轮请求。

**Q：双击 `启动.cmd` 出现乱码或"不是内部或外部命令"？**
早期版本的启动脚本包含中文且以 UTF-8 保存，中文控制台（GBK 编码）解析时会字节错位导致乱码。当前版本已改为纯 ASCII 脚本并自动检测 Node.js，不会再出现此问题。如果仍有异常，可在命令行进入本目录后执行 `node server.js` 作为替代启动方式。

**Q：双击后提示"端口已被占用"？**
说明服务已经在运行了（可能之前启动过）。直接用浏览器打开 http://127.0.0.1:8765 即可使用，无需重复启动。

**Q：打开页面显示不出数据？**
确认本地服务已启动（浏览器访问 http://127.0.0.1:8765/api/ping 应返回 JSON）。若显示「本地模式」，说明页面能打开但服务未响应，重启 `启动.cmd` 即可。

**Q：端口 8765 被占用？**
编辑 `server.js` 顶部的 `const PORT = 8765;` 改成其他端口（如 8766），重启后访问新端口。

**Q：想换基金？**
直接在输入框填新的 6 位代码加载即可。不同基金的买入记录互相独立，切换时自动筛选显示。

**Q：点「详情」按钮没反应？**
「详情」是跳转到**天天基金**的站外页面，需要本机能访问外网；另外它默认打开新标签页，若浏览器拦截弹窗则会改为在当前页跳转。本应用自身依然是纯本地的，跳转只在你点击时发生。

**Q：数据会上传到云端吗？**
不会。所有数据只存在你本机的 `data/` 目录和浏览器本地。联网只用于**读取**公开的净值数据，不上传任何个人信息。本地服务也只接受来自 `127.0.0.1` / `localhost` 的请求，其他网站无法读取你的持仓。

**Q：为什么分红后我的涨跌幅没有跟着净值一起掉？**
这是**故意**的。分红后单位净值下跌、但现金已进入你的口袋，所以所有涨跌幅按含分红的复权口径计算（分红被算进收益）。只有均线和最大回撤仍是单位净值口径。

**Q：为什么基金列表没有「浮动盈亏」了？**
已改成两个更直接的口径：**相较于前次买入**、**相较于前次卖出**（当前净值 vs 最后一次买入/卖出价）。没有对应记录时那一格是空的（—）。图表的买卖点旁也改成同样思路的两行数字。

**Q：数据文件在哪里？能直接改吗？**
`data/records.json`（记录）、`data/navCache.json`（净值缓存）、`data/backups/`（自动快照）。建议不要手改 JSON；迁移电脑时用界面上的「备份 / 导入」更稳妥。

**Q：提示"数据冲突"是什么意思？**
你有两个窗口（或两个标签页）同时在改数据。选"确定"用当前页面的数据覆盖，选"取消"加载另一边的数据。任何一侧都不会被静默丢弃。

**Q：提示"记录文件损坏"怎么办？**
程序已把损坏文件改名保留（`data/records.json.corrupt-*.bak`），**没有**覆盖它。可以试着用文本编辑器打开该文件抢救内容，或用之前的 `data/backups/` 快照 / 导出的备份文件恢复（把快照改名为 `records.json` 放到 `data/` 下，重启即可）。

**Q：旧版本的 `data/store.json` 呢？**
首次启动会自动拆分迁移为 `records.json` + `navCache.json`，原文件改名为 `store.json.migrated-*.bak` 保留，确认无误后可自行删除。

**Q：能多台电脑同步吗？**
当前为纯本地应用。换机器时用「备份」导出 JSON，在新机器「导入」即可迁移。

**Q：移动端能用吗？**
页面本身响应式（窄屏单列、表格转卡片、适配 iPhone 底部安全区，窄屏可用），但未做手机端的专门交互规则；拖拽排序仅桌面端可用。已知边界：记录类型仅买入/卖出两种，卖出时不会校验份额是否超过持仓。

**Q：为什么卖出记录里的金额和份额有时对不上？**
卖出金额即卖出回款，份额 = 金额 ÷ 当日净值。非交易日（周末/节假日）会取此前最近一个交易日的收盘净值并在净值后标 `*`，因此与"当天"直觉可能有差异。

---

## 十三、免责声明

本工具仅用于**个人记录与数据展示**，所有净值数据来自第三方公开接口，不保证准确性与及时性，不构成任何投资建议。收益计算为近似口径（含现金分红修正，未计申赎费、税费与红利再投），请以基金公司对账单为准。顾比买卖建议由固定规则从均线形态推得，仅供参考。

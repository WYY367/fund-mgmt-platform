# 基金买入工作台

一个用于**指导基金操作**的本地 APP：记录你的每次买入与卖出，并在真实净值走势曲线上标出买卖点，一眼看清每个买卖点相对**上一次买入 / 上一次卖出**的涨跌、**当前净值较前次操作的涨跌幅**，以及**均线走势与区间回撤**。

---

## 一、快速开始

### 启动

**方式一（推荐）**：双击目录里的 `启动.cmd`

**方式二**：命令行执行

```bash
node server.js
```

看到下面的提示就说明启动成功：

```
====================================================
  基金买入工作台 已启动
====================================================
  访问地址：http://127.0.0.1:8765
====================================================
```

然后用浏览器打开 **http://127.0.0.1:8765**

> 首次启动后建议把该地址加入浏览器书签，下次直接点书签即可（服务需保持运行）。

### 使用三步走

1. **选基金** —— 在「选择基金」输入框填 6 位基金代码（如 `110022`），点「加载净值」。支持输入名称片段搜索。
2. **加记录** —— 在「买入记录」里先选类型（**买入/卖出**），填日期、金额，点「添加买入」。
3. **看图表** —— 上方曲线立即出现你的买卖点，每个点标注**相对上一次买入的涨跌幅**。

### 收益口径（先看这里）

本工具**不计算总体盈亏**，只看「**当前净值相对上一次操作点**」涨了多少、跌了多少，两个口径：

- **相较于前次买入** = 最新净值 ÷ 最后一次买入的当日净值 − 1
- **相较于前次卖出** = 最新净值 ÷ 最后一次卖出的当日净值 − 1

没有买入（或卖出）记录时，对应位置**留空显示 —**。所有涨跌幅都按**含分红的复权口径**计算（用累计净值扣掉分红除权的影响）。

> 例：最后一次买入价 1.000、最后一次卖出价 1.200、最新净值 1.100 → 相较于前次买入 +10.0%，相较于前次卖出 -8.3%。

### 卖出记录

- 录入表单的类型切换为「卖出」时，金额即卖出回款，份额按当日净值折算
- 记录表用徽章区分**买入 / 卖出**；卖出点在图上为橙色菱形
- 概览卡片显示：操作笔数（买 X · 卖 Y）、投入本金（附卖出回款）、相较于前次买入、相较于前次卖出

### 批量新增（多基金批量买入/卖出）

点击「买入记录」卡片右上角的**批量新增**：

1. 选择类型（批量买入 / 批量卖出）
2. 填日期、每只金额、备注
3. 勾选要操作的基金（可多选）
4. 点「添加到所选基金」——一次为所有勾选基金创建相同记录

与「批量管理」（批量修改/删除）相互独立，随时切换。

### 我的基金（表格管理）

「我的基金」以表格展示**所有录入过记录及已加载的基金**，列包括：

| 列 | 说明 |
|---|---|
| 基金 | 名称 + 代码，**点击名称切换**到该基金的图表与记录 |
| 笔数 | 买入记录条数 |
| 相较于前次买入 | 最新净值相对**最后一次买入**净值的涨跌幅（含分红）；没有买入记录则留空 |
| 相较于前次卖出 | 最新净值相对**最后一次卖出**净值的涨跌幅（含分红）；没有卖出记录则留空 |
| 成立以来最大回撤 | 该基金净值相对历史最高点的最深回撤 |
| 顾比信号 | 最近 **5 个交易日**内出现顾比均线**金叉**（红▲，短期组均值上穿长期组均值）或**死叉**（绿▼，下穿）时在此标记提示；无信号或历史不足 61 个交易日时留空 |
| 操作 | **置顶**（星标，永远排最前）· **详情**（新标签页打开天天基金该基金页面）· **更新**（刷新净值）· **删**（删除基金及其全部记录，需确认） |

排序与顺序：

- **拖拽** 行首的 ⋮ 手柄可调整顺序（仅在手动模式下）
- 点击「相较于前次买入」「相较于前次卖出」或「成立以来最大回撤」表头，**按数值从大到小**排列；再点一次切换为从小到大；第三次点击恢复手动拖拽模式（留空的基金恒排在最后）
- 置顶的基金始终排在最上方
- 排序结果、置顶、顺序都会自动保存

### 查看基金详情（跳转数据源）

点每行的 **详情** 按钮 → 新标签页打开**天天基金**上该基金的页面（`fund.eastmoney.com/<基金代码>.html`），可以看净值走势、阶段涨幅、评级、规模、基金经理、分红与公告。

> 该页面属于站外页面，需要本机可访问外网；如果你的浏览器拦截了新窗口，会改为在当前页跳转。

---

## 二、核心功能

### 净值走势 + 买入点标注（核心）

图表横轴是日期、纵轴是单位净值，折线是基金的真实净值曲线。每个买入点（红圆点）与卖出点（橙菱形）旁都会标**两行**数字：

```
买 +15.0%      ← 相对「上一次买入」
卖  -9.1%      ← 相对「上一次卖出」
```

- **买** 行 = 该点净值相对**上一次买入**点（追高为红、抄底为绿；颜色只作用于数字）
- **卖** 行 = 该点净值相对**上一次卖出**点
- 若某一边没有"上一次"（比如第一笔买入没有更早的买入），基准自动取**当前所选时间段的第一个交易日**，该行会标出区间名，例如 `买(近3月) +4.2%`；若基准就是该点自己，显示 `基准`

> **口径说明（重要）**：所有涨跌幅都**按含分红的复权口径**计算（用"累计净值"扣除分红除权的影响）——基金分红后单位净值会下跌，若只看单位净值会把分红误判成亏损或"抄底"。份额仍按**单位净值**折算（真实买入价），不含申购/赎回费。均线与最大回撤仍是单位净值口径，分红除权日会有跳空。

区间可切换 **近1月 / 近3月 / 近6月 / 近1年 / 近3年 / 近5年 / 近10年 / 全部**。其中「全部」= **基金成立至今**的完整净值历史（首次打开页面时会自动把旧数据升级为全量历史）。切换区间会同时改变"区间首个交易日"这个兜底基准。

**标签自动避让**：标签位置由程序自动挑选——优先放在标记点的右上/右下/左上/左下，若该处压到曲线、其他标记或别的标签，会自动换边、拉开距离或上下错开，尽量不遮挡曲线与买卖点。只有当多笔操作挤在同几天时才可能出现轻微重叠（把鼠标悬停到点上可看清明细）。

**悬停查看详情**：鼠标在图上移动时会出现**十字准线**，跟随浮层显示对应交易日的日期、单位净值（含较昨日涨跌）、**最大回撤**（该日净值相对**当前所选时间段内**最高点的回撤幅度，处于区间高点时为 0，切换区间会重算）、**均线读数**（随当前均线模式变化，见下），以及 **较上次买入 / 较上次卖出**两行涨跌幅（无上次同类操作时，基准为所选区间首个交易日，浮层会注明）。移动到买入点/卖出点上时，浮层额外显示该笔的金额、份额、持有至最新（卖出点还显示剩余份额）。

**均线模式**：图例右侧有两档切换按钮——

| 模式 | 内容 |
|---|---|
| **顾比均线**（默认） | 短期组 EMA 3/5/8/10/12/15（青色组带 + 6 条细线）、长期组 EMA 30/35/40/45/50/60（品红组带 + 6 条细线），金叉标红▲、死叉标绿▼ |
| 关闭 | 只看净值曲线与买卖点 |

**顾比买卖建议**：使用顾比均线时，图下方会给出当前的操作建议（买入 / 持有 / 观望 / 减仓 / 卖出）及其理由，依据包括：价格相对长期组的位置（支撑 / 压力）、两组均线的方向（斜率）、短期组发散 / 收敛、近期金叉 / 死叉等形态。长线趋势稳定时回调到长期组附近被视为低吸机会；短期组下穿长期组且长期组向下时提示离场。

> **建议仅供参考**，由固定规则从均线形态推得，不构成投资建议；数据不足 60 个交易日时不给建议。

**图例**：基金净值 · 顾比均线（短期组 / 长期组，及金叉 / 死叉标记，随模式变化）· 买入点 · 卖出点，另有「较前次买入 / 较前次卖出」的当前读数与口径说明。

### 今天要处理

页面最上方自动列出需要关注的事：

- 净值数据过期（超过 3 天未更新）→ 一键刷新
- 有买入记录但净值未加载 → 一键加载
- 最近一次买入后跌幅超过 10% → 提示可考虑补仓
- 没有异常时显示「一切正常」

### 数据概览

四张卡片一目了然：**操作笔数**（附「几次买在更低位」统计）、**投入本金**（附卖出回款）、**相较于前次买入**、**相较于前次卖出**（后两张以最新净值为基准，卡片下方注明基准日期）。

### 批量管理（跨基金）

点击「买入记录」卡片右上角的**批量管理**按钮进入批量模式：

- 表格列出**所有基金**的买入记录，可按基金筛选
- 支持**全选 / 清除 / 逐条勾选**
- 对选中的记录可一次性执行：
  - **改日期** —— 统一改为指定日期
  - **改金额** —— 统一改为指定金额
  - **改备注** —— 统一改为指定备注（留空则清空）
  - **批量删除** —— 一次性删除（需二次确认）
- 再点一次「退出批量」回到普通模式

### 买卖记录表

每笔记录显示：日期、金额、当日净值、份额、备注、**较上次买入**、**较上次卖出**、**持有至最新**。支持修改和删除。

- **较上次买入**：这笔操作相对**上一次买入**净值的涨跌幅（首笔买入显示「基准」）
- **较上次卖出**：这笔操作相对**上一次卖出**净值的涨跌幅（首笔卖出显示「基准」，此前还没有卖出记录的行显示「—」）
- **持有至最新**：仅买入记录显示（该笔买入价相对最新净值）

> 若买入日是非交易日（周末/节假日），会自动取此前最近一个交易日的收盘净值，并在净值旁标注 `*`。

---

## 三、数据存储

数据按"能不能再生"分开存放，重启不会丢：

| 存储位置 | 说明 |
|---------|------|
| `data/records.json` | **主存储**：你的买卖记录（只有几 KB） |
| `data/navCache.json` | 净值缓存，属于可重建数据，删掉后点「加载净值」即可恢复 |
| `data/backups/` | 每次写入前自动生成的记录快照，保留最近 8 份 |
| 浏览器 localStorage | 兜底备份（只存记录），服务未启动时也能保留 |

顶部右上角有同步状态指示：**已保存到本地** / **保存中** / **本地模式** / **保存失败**。

- **多窗口同时用**：另一个窗口改了数据，本窗口会自动同步；万一真撞车了，会弹窗让你选"用本页数据覆盖"还是"加载对方数据"，不会悄悄丢掉一边。
- **文件损坏**：程序会把损坏文件改名保留为 `*.corrupt-*.bak` 并提示你，**不会**用示例数据把它顶掉。
- **旧版本升级**：老版的 `data/store.json` 会在首次启动时自动拆分迁移，原文件改名为 `store.json.migrated-*.bak` 保留（确认无误后可自行删除）。

### 备份与恢复

- **备份** —— 点右上角「备份」，导出 `基金工作台备份_日期.json`。含全部基金净值和买卖记录。
- **导入** —— 点右上角「导入」，选择 JSON 文件即可恢复，条数无上限。

> 建议数据积累到 30 条以上时导出一次备份（应用会主动提醒）。

---

## 四、数据来源

净值数据来自**天天基金（东方财富）**公开接口，权威且及时：

- 单位净值与累计净值时间序列（`pingzhongdata` 接口）
- 基金代码校验与搜索（`FundSearch` 接口）

由于浏览器有跨域限制，`server.js` 作为本地代理转发请求 —— 这也是必须启动本地服务的原因。

数据覆盖**该基金成立以来**的全部交易日净值（不是"最近几年"），每个交易日收盘后更新。

> 上游若某只基金没提供累计净值，该基金的涨跌幅会自动退回单位净值口径（不影响使用，只是不体现分红）。

---

## 五、常见问题

**Q：双击 `启动.cmd` 出现乱码或"不是内部或外部命令"？**
早期版本的启动脚本包含中文且以 UTF-8 保存，中文控制台（GBK 编码）解析时会字节错位导致乱码。当前版本已改为纯 ASCII 脚本并自动检测 Node.js，不会再出现此问题。如果仍有异常，可在命令行进入本目录后执行 `node server.js` 作为替代启动方式。

**Q：双击后提示"端口已被占用"？**
说明服务已经在运行了（可能之前启动过）。直接用浏览器打开 http://127.0.0.1:8765 即可使用，无需重复启动。

**Q：打开页面显示不出数据？**
确认本地服务已启动（浏览器访问 http://127.0.0.1:8765/api/ping 应返回 JSON）。若显示「本地模式」，说明页面能打开但服务未响应，重启 `启动.cmd` 即可。

**Q：端口 8765 被占用？**
编辑 `server.js` 顶部的 `const PORT = 8765;` 改成其他端口（如 8766），重启后访问新端口。

**Q：想换基金？**
直接在输入框填新的 6 位代码加载即可。不同基金的买入记录互相独立，切换时自动筛选显示。

**Q：点「详情」按钮没反应？**
「详情」是跳转到**天天基金**的站外页面，需要本机能访问外网；另外它默认打开新标签页，若浏览器拦截弹窗则会改为在当前页跳转。本应用自身依然是纯本地的，跳转只在你点击时发生。

**Q：数据会上传到云端吗？**
不会。所有数据只存在你本机的 `data/` 目录和浏览器本地。联网只用于**读取**公开的净值数据，不上传任何个人信息。本地服务也只接受来自 `127.0.0.1` / `localhost` 的请求，其他网站无法读取你的持仓。

**Q：为什么分红后我的涨跌幅没有跟着净值一起掉？**
这是**故意**的。分红后单位净值下跌、但现金已进入你的口袋，所以所有涨跌幅按含分红的复权口径计算（分红被算进收益）。只有均线和最大回撤仍是单位净值口径。

**Q：为什么基金列表没有「浮动盈亏」了？**
按你的要求改成了两个更直接的口径：**相较于前次买入**、**相较于前次卖出**（当前净值 vs 最后一次买入/卖出价）。没有对应记录时那一格是空的（—）。图表的买卖点旁也改成同样思路的两行数字。

**Q：数据文件在哪里？能直接改吗？**
`data/records.json`（记录）、`data/navCache.json`（净值缓存）、`data/backups/`（自动快照）。建议不要手改 JSON；迁移电脑时用界面上的「备份 / 导入」更稳妥。

**Q：提示"数据冲突"是什么意思？**
你有两个窗口（或两个标签页）同时在改数据。选"确定"用当前页面的数据覆盖，选"取消"加载另一边的数据。任何一侧都不会被静默丢弃。

**Q：提示"记录文件损坏"怎么办？**
程序已把损坏文件改名保留（`data/records.json.corrupt-*.bak`），**没有**覆盖它。可以试着用文本编辑器打开该文件抢救内容，或用之前的 `data/backups/` 快照 / 导出的备份文件恢复（把快照改名为 `records.json` 放到 `data/` 下，重启即可）。

**Q：能多台电脑同步吗？**
当前为纯本地应用。换机器时用「备份」导出 JSON，在新机器「导入」即可迁移。

**Q：移动端能用吗？**
页面本身响应式（窄屏单列、表格转卡片、适配 iPhone 底部安全区，窄屏可用），但未做手机端的专门交互规则；拖拽排序仅桌面端可用。已知边界：记录类型仅买入/卖出两种，卖出时不会校验份额是否超过持仓。

---

## 六、文件说明

```
基金买入工作台/
├── 启动.cmd              # 双击启动（Windows）
├── server.js             # 本地服务：静态托管 + 数据代理 + 分级持久化 + 访问控制
├── public/
│   └── index.html        # 应用页面（单文件，全部内联）
├── data/                 # 你的数据（自动生成，勿手改）
│   ├── records.json      # 买卖记录（主）
│   ├── navCache.json     # 净值缓存（可再生）
│   └── backups/          # 记录自动快照
└── 使用说明.md            # 本文档
```

---

## 七、技术说明

- 单文件 HTML，CSS/JS 全部内联，**零外部依赖**（无框架、无 CDN、无字体外链）
- 图表为**手写内联 SVG** 绘制，图标全部内联 SVG，不使用 emoji
- 服务端仅用 Node 内置模块（`http`/`https`/`fs`/`path`），**无需 npm install**
- 记录与净值缓存**分开存储**：记录（不可再生）有双写兜底、自动快照与并发校验；净值缓存（可再生）只落本地文件、单只增量写入
- 涨跌配色遵循国内习惯：**涨红跌绿**，货币统一 ¥
- 图表默认叠加**顾比均线（GMMA）**：短期组 EMA 3/5/8/10/12/15、长期组 EMA 30/35/40/45/50/60，可关闭；长序列（>400 个净值点）对均线自动抽稀采样以控制 SVG 体积
- 涨跌幅采用**含分红的复权口径**；份额按单位净值折算，不含申赎费；不计算总体盈亏，只给「较前次买入 / 较前次卖出」两个口径
- 拖拽排序依赖浏览器原生拖放，仅桌面端可用（页面内不得出现把 `-webkit-user-drag` 置为 `none` 的全局样式，否则拖拽会失效）
- 已适配移动端浏览（窄屏单列、表格转卡片、按钮≥44px），但第一版未做手机端的专门交互规则
